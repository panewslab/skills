interface:
  display_name: "PANews"
  short_description: "Structured PANews news discovery across articles, rankings, topics, columns, series, events, and calendars"
  default_prompt: "Use $panews for structured PANews news discovery, including briefings, article search, rankings, topics, columns, series, events, calendars, and editorial picks."

policy:
  allow_implicit_invocation: true
